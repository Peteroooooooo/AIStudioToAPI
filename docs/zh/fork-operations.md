# 独立版运维说明

本分支基于 v1.3.5 源码，包含 `FormatConverter.js` 补丁。镜像由本仓库构建，仅发布 `linux/arm64` 到 `ghcr.io/peteroooooooo/aistudio-to-api`，使用 Docker v2 镜像清单格式兼容 Docker 20.10。部署时使用版本标签或完整提交 SHA 标签，避免依赖 `latest`。

## 账号健康规则

| 回包            | 自动处理                                                  |
| --------------- | --------------------------------------------------------- |
| 429             | 当前账号立即冷却 5 分钟；再次发生时逐级增加，最多 40 分钟 |
| 401             | 10 分钟内两个不同请求均失败后标记为“需重新登录”           |
| 502/503/504     | 10 分钟内连续三次失败后冷却，从 1 分钟起逐级增加          |
| 400/403/404/422 | 直接返回给调用方，不禁用账号，不重复尝试                  |

账号状态保存在 `/app/data/account-health.json`，不会修改认证 cookie。控制台账户列表可手动停用、启用或重置健康状态。冷却结束后账号自动重新参与轮换；“需重新登录”和手动停用必须由管理员重置。后台会在隔离账户后重新平衡浏览器上下文，正在处理请求的上下文会延后关闭。

`/health` 是进程存活检查，始终保留原有语义。`/health/ready` 仅在当前账号健康且 WebSocket 已连接时返回 200，否则返回 503。调用端在所有账号不可用时会收到明确的 503。

## 运行参数热更新

在控制台“设置”页调整浏览器上下文上限、单次请求总尝试次数、账号失败阈值、账号使用次数阈值、重试间隔和请求超时，然后保存即可应用，无需重建容器。原有的流式模式、强制思考、联网和安全阈值等开关也保存到同一份配置。页面会同时显示当前值和首次导入的基准值；“恢复初始值”将可热更新的设置恢复到文件中记录的基准值。

`/app/data/config.json` 是运行配置的单一来源，因此 Portainer Stack 需要保持 `/app/data` 的持久卷挂载。首次没有该文件时，服务把旧环境变量导入文件一次；之后修改 Portainer 环境变量不会覆盖文件。文件的 `settings` 包含可热更新参数，`startup` 包含端口、监听地址、浏览器程序、API 密钥及控制台密码等启动项。直接编辑 `startup` 后需重启容器，敏感值不会经网页配置接口回显。

`MAX_RETRIES` 表示包含首次调用在内的总尝试次数。`MAX_CONTEXTS=0` 表示不限制预热上下文数量，可能显著增加内存。调整上下文上限会启动后台重平衡，正在处理请求的上下文会在空闲后关闭。

### Gemini 显式缓存

设置页可热更新缓存开关、资源有效期（默认 1 小时，可设 60 秒至 7 天）、续期窗口（默认 0，即不自动续期）、最小前缀 1024 Token、历史检查点间隔（默认 1024 Token）和资源上限。本地实测 `gemini-3.8-flash` 上游拒绝 1023 Token 的缓存，接受并实际命中 1024 Token 的缓存。修改有效期只影响新建和续期的资源，不会立即改掉已有资源的到期时间；到期后会按原请求发送完整历史。缓存索引保存在 `/app/data/gemini-cache-index.json`，只含账号标识和请求前缀哈希、上游资源名、时间与计数，不含提示词明文。它依赖 `/app/data` 持久挂载；旧索引丢失不会影响请求正确性，只会降低命中率。

客户端照常发送完整请求。服务按实际 Google 账号、模型、系统提示、工具和历史消息寻找最长前缀；新内容仍逐次发送给 Gemini。相同系统提示和工具可供不同新 session 共享，但变更任何固定内容、模型或账号会生成另一份缓存。新建更长的历史前缀缓存后，会替换同一路径上较短的历史缓存并清理上游资源；正在使用的资源会等请求结束，并发中的分叉对话若仍在使用短前缀，该前缀会保留；纯系统提示和工具的共享前缀不会被历史缓存替换。若原账号仍健康且浏览器连接存在，请求会优先回到持有更长缓存的账号；不可用时使用当前账号发完整请求。失败的生成请求不会建缓存；上游拒绝缓存资源时清除本地映射并回退到原请求。后台维护缓存的调用不参与账号健康计数。

页面和兼容 API 中的缓存 Token 来自 Gemini 上游 `cachedContentTokenCount`，未报告时不推测命中。对 Claude 协议，`input_tokens` 表示未缓存输入，`cache_read_input_tokens` 表示缓存输入。关闭缓存只停止使用与新建，已有上游资源可待自然到期；变短的 TTL 对既有资源在下次续期时生效。

## 部署与回滚

浏览器上下文是主要内存来源。先按主机内存预算设置初始 `MAX_CONTEXTS`，再在控制台观察内存与账号切换延迟后调整。清理 Linux 文件缓存不会减少浏览器常驻内存。

切换前保存现用镜像的标签或 digest、Compose 配置，以及认证和数据目录的备份。认证文件应单独保管，不要打包进镜像或提交到仓库。

先在隔离环境验证新镜像及 `/health/ready`。单机替换容器会有短暂连接中断，应在允许切换的时间窗口修改 Compose 的镜像标签，然后重新创建容器。验证 API 格式、工具调用、429 冷却、管理界面和内存。回滚时恢复先前的镜像与 Compose 配置；如需恢复原有运行参数，还要恢复 `/app/data/config.json` 的备份。

推送 `stable` 或版本标签会运行测试、JS lint 和前端构建；只有版本标签会构建 arm64 镜像，并同时发布版本标签和完整提交 SHA 标签。部署时建议记录镜像 digest 以固定实际内容。镜像必须构建完成后才能部署；首次发布还需确认 GHCR 包的可见性及目标主机的拉取权限。
